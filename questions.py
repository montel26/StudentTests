# 1. Two Sum Problem
def two_sum(nums, target):
    """
    TODO:
    - Iterate through the list `nums`.
    - For each element, calculate the complement (i.e., target - num).
    - Check if the complement exists in a map/dictionary.
    - If yes, return the indices of the current element and the complement.
    - Otherwise, store the element with its index in the map.
    - Return the indices as the result.
    """
    pass


# 2. Remove Duplicates from Sorted Array
def remove_duplicates(nums):
    """
    TODO:
    - Traverse the sorted array `nums`.
    - Keep a pointer to store the position of unique elements.
    - For each unique element, place it at the pointer's position and increment the pointer.
    - Return the total number of unique elements.
    """
    pass


# 3. Reverse an Array
def reverse_array(nums):
    """
    TODO:
    - Reverse the array `nums` in place.
    - Use Python's built-in reverse methods or a manual approach.
    - Return the reversed array.
    """
    pass


# 4. Find Maximum in an Array
def find_max(nums):
    """
    TODO:
    - Iterate through the array `nums`.
    - Keep track of the maximum element.
    - Return the largest element in the array.
    """
    pass


# 5. Find Minimum in an Array
def find_min(nums):
    """
    TODO:
    - Iterate through the array `nums`.
    - Keep track of the minimum element.
    - Return the smallest element in the array.
    """
    pass


# 6. Check for Palindrome
def is_palindrome(s):
    """
    TODO:
    - Reverse the string `s`.
    - Compare the original string with the reversed one.
    - Return True if they are the same, otherwise return False.
    """
    pass


# 7. Find First Unique Character in a String
def first_unique_char(s):
    """
    TODO:
    - Count the occurrences of each character in the string `s`.
    - Iterate through the string and return the index of the first character that appears only once.
    - If no unique character is found, return -1.
    """
    pass


# 8. Fibonacci Sequence
def fibonacci(n):
    """
    TODO:
    - Implement an iterative or recursive function to calculate the nth Fibonacci number.
    - For n = 0, return 0.
    - For n = 1, return 1.
    - For n > 1, calculate the Fibonacci sequence up to n and return the nth number.
    """
    pass


# 9. Find Common Elements in Two Arrays
def find_common_elements(arr1, arr2):
    """
    TODO:
    - Convert both arrays into sets.
    - Find the intersection of the two sets.
    - Return the common elements as a list.
    """
    pass


# 10. Move Zeroes to End
def move_zeroes(nums):
    """
    TODO:
    - Traverse through the list `nums`.
    - For each non-zero element, shift it towards the front.
    - Move all zeroes to the end of the array, while maintaining the relative order of other elements.
    - Return the modified array.
    """
    pass


# 11. Count Vowels in a String
def count_vowels(s):
    """
    TODO:
    - Iterate through the string `s`.
    - For each character, check if it is a vowel (a, e, i, o, u - both uppercase and lowercase).
    - Count the number of vowels and return the count.
    """
    pass


# 12. Merge Two Sorted Arrays
def merge_sorted_arrays(arr1, arr2):
    """
    TODO:
    - Merge the two sorted arrays `arr1` and `arr2` into a single sorted array.
    - Use a sorting algorithm or Python's built-in sorting method.
    - Return the merged sorted array.
    """
    pass
